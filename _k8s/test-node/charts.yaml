apiVersion: v2
name: cardano-test-node
description: A Helm chart for deploying the Cardano node with custom configurations
type: application
version: 0.1.0
appVersion: "1.0.0"  # Adjust the app version if needed
